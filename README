fetching data within the same component
fetching the data through fake API and passing it from one component to other 
created a fake API and fetching the data it and passed it from one component to other 

